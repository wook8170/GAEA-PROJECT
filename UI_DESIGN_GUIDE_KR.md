# OpenProject UI 디자인 가이드 (GAEA-PROJECT)

이 문서는 OpenProject(GAEA-PROJECT)의 UI 컴포넌트와 디자인 시스템에 대한 가이드를 제공합니다. 본 시스템은 GitHub의 **Primer Design System**을 기반으로 하며, OpenProject만의 고유한 패턴과 커스텀 스타일이 추가되었습니다.

---

## 1. 디자인 철학 및 접근 방식

OpenProject는 강력한 기능과 사용자 정의 가능성을 제공하는 복잡한 도구입니다. 우리의 디자인 목표는 **"강력함과 접근성 사이의 균형"**을 맞추는 것입니다.

-   **기본값 우선 (Sane Defaults)**: 일반적인 사용자에게는 가장 직관적인 옵션을 먼저 제공합니다.
-   **단계적 정보 공개 (Progressive Disclosure)**: 고급 설정은 한 번 더 클릭하여 접근할 수 있도록 하여 신규 사용자의 인지 부하를 줄입니다.
-   **일관성 (Coherence)**: 오랜 역사를 가진 프로젝트인 만큼, 점진적으로 모든 UI를 Primer 기반의 현대적인 디자인 언어로 통합하고 있습니다.

---

## 2. 디자인 시스템 기초 (Primer)

우리는 GitHub의 [Primer Design System](https://primer.style/)을 핵심 프레임워크로 사용합니다.

-   **Primer CSS / ViewComponents**: Rails 백엔드에서는 Primer ViewComponents를, 프론트엔드에서는 Angular와 Primer 스타일을 혼합하여 사용합니다.
-   **디자인 토큰 (Design Tokens)**: 색상, 간격, 타이포그래피 등을 직접 하드코딩하지 않고 Primer에서 제공하는 변수(CSS Variables)를 사용합니다.

---

## 3. 레이아웃 및 구조

페이지 레이아웃은 일관된 사용자 경험을 위해 정해진 패턴을 따릅니다.

### 3.1 레이아웃 컴포넌트 (`Primer::Beta::Layout`)
대부분의 관리 및 설정 페이지는 다음과 같은 구조를 가집니다.
-   **사이드 패널 (Side Panel)**: 내비게이션 메뉴 및 필터가 위치합니다.
-   **메인 콘텐츠 area (Main Content)**: 실제 데이터와 작업 내용이 표시됩니다.
-   **반응형 대응**: 모바일 환경에서는 사이드 패널이 하단으로 쌓이거나 햄버거 메뉴로 통합됩니다.

---

## 4. 색상 시스템 (Colors)

OpenProject는 다크 모드와 고대비 모드를 지원하며, 사용자가 테마 디자인을 커스터마이징할 수 있습니다.

### 4.1 핵심 색상
| 변수                     | 기본값 (Hex) | 용도                                                               |
| :----------------------- | :----------- | :----------------------------------------------------------------- |
| `--primary-button-color` | `#1F883D`    | 가장 중요한 동작 (생성, 저장 등)에 제한적으로 사용되는 녹색입니다. |
| `--accent-color`         | `#1A67A3`    | 링크, 선택된 상태, 강조 요소 등에 사용되는 파란색입니다.           |
| `--header-bg-color`      | `#1A67A3`    | 상단 헤더의 배경색입니다.                                          |

### 4.2 접근성 (Accessibility)
-   모든 색상 조합은 웹 접근성 표준을 준수해야 합니다.
-   다크 모드에서는 커스텀 강조 색상이 배경과 잘 어우러지도록 자동으로 밝기가 조정됩니다.

---

## 5. 아이콘 및 타이포그래피

### 5.1 아이콘 (`Octicons`)
-   Primer에서 제공하는 [Octicons](https://primer.style/foundations/icons/)를 기본 아이콘 세트로 사용합니다.
-   새로운 기능을 추가할 때 기존 스타일과 어긋나지 않도록 Octicons 목록에서 적절한 아이콘을 선택해야 합니다.

---

## 6. 주요 UI 패턴

### 6.1 폼 (Forms)
-   **입력 필드**: 레이블은 항상 입력창 위에 위치해야 합니다.
-   **인플레이스 편집 (In-place Edit)**: 작업 패키지 상세 보기와 같이 데이터를 클릭하여 즉시 수정할 수 있는 패턴을 적극적으로 사용합니다.

### 6.2 대화 상자 및 모달 (Dialogs)
-   중요한 확인 작업이나 별도의 컨텍스트가 필요한 경우 Primer의 `Modal` 또는 `Dialog` 컴포넌트를 사용합니다.

### 6.3 알림 배너 (Flash Banners)
-   **공지/성공/오류**: 페이지 상단에 고정된 플래시 배너를 통해 사용자에게 상태 변경을 알립니다.
    -   성공: 녹색(Success)
    -   오류: 빨간색(Danger)
    -   경고: 노란색(Warning)

---

## 7. 가이드라인 준수 확인 사항

새로운 UI 컴포넌트를 개발하거나 수정할 때 다음 질문을 검토하십시오.
1.  Primer 디자인 토큰(변수)을 사용했는가?
2.  다크 모드와 고대비 모드에서도 가독성이 확보되는가?
3.  기존 오픈프로젝트의 다른 페이지와 레이아웃 일관성이 있는가?
4.  반응형 환경(모바일/태블릿)에서 정상적으로 작동하는가?

---

더 자세한 컴포넌트 미리보기와 사양은 로컬 개발 환경의 **Lookbook**(`http://localhost:3000/lookbook`)에서 직접 확인할 수 있습니다.
